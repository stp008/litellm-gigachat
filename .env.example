# Пример файла переменных окружения для GigaChat LiteLLM интеграции
# Скопируйте этот файл в .env и заполните своими значениями

# Authorization Key для GigaChat API (обязательно)
# Получите в личном кабинете: https://developers.sber.ru/studio/workspaces
# Перейдите в проект GigaChat API -> Настройки API -> Получить ключ
GIGACHAT_AUTH_KEY=your_base64_authorization_key_here

# Дополнительные настройки (опционально)

# Область действия токена (по умолчанию: GIGACHAT_API_PERS)
# GIGACHAT_SCOPE=GIGACHAT_API_PERS

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Хост для прокси-сервера (по умолчанию: 0.0.0.0)
# PROXY_HOST=0.0.0.0

# Порт для прокси-сервера (по умолчанию: 4000)
# PROXY_PORT=4000

# Таймаут для запросов к GigaChat API в секундах (по умолчанию: 60)
# GIGACHAT_TIMEOUT=60

# Буфер времени для обновления токена в секундах (по умолчанию: 300 = 5 минут)
# TOKEN_REFRESH_BUFFER=300

# Установка российских корневых сертификатов (по умолчанию: false)
# Включите только для публичного GigaChat API с доступом в интернет
# На внутренних стендах без интернета оставьте false
# INSTALL_RUSSIAN_CERTS=false

# ========================================
# Настройки прокси-провайдера (кастомный API endpoint)
# ========================================

# Включить поддержку прокси-провайдера (по умолчанию: false)
# Работает с любыми моделями через кастомный API endpoint
PROXY_PROVIDER_ENABLED=true

# URL прокси-провайдера (обязательно при PROXY_PROVIDER_ENABLED=true)
PROXY_PROVIDER_URL=https://my-api.company.com/api/v1

# Название заголовка для аутентификации (по умолчанию: X-Client-Id)
PROXY_PROVIDER_AUTH_HEADER=X-Client-Id

# Значение заголовка для аутентификации (обязательно при PROXY_PROVIDER_ENABLED=true)
PROXY_PROVIDER_AUTH_VALUE=bddcba1a-6139-4b5f-9994-90f1b74e9109

# Суффикс для имен моделей прокси-провайдера (по умолчанию: proxy)
PROXY_PROVIDER_MODEL_SUFFIX=proxy

# ========================================
# Автоматическая синхронизация моделей
# ========================================

# Включить автоматическую синхронизацию моделей (по умолчанию: false)
# Работает только при PROXY_PROVIDER_ENABLED=true
MODEL_SYNC_ENABLED=true

# Интервал синхронизации моделей в секундах (по умолчанию: 300 = 5 минут)
MODEL_SYNC_INTERVAL=300

# Суффикс берётся из PROXY_PROVIDER_MODEL_SUFFIX автоматически
