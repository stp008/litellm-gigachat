model_list:
  # Модели прокси-провайдера (mock-сервер)
  # Эти модели будут автоматически синхронизированы при MODEL_SYNC_ENABLED=true
  # Или можно добавить вручную для тестирования без синхронизации
  
  # - model_name: llama-3.1-70b-proxy
  #   litellm_params:
  #     model: openai/llama-3.1-70b
  #     api_base: http://localhost:8000/v1
  #     api_key: "none"
  #     timeout: 60
  
  # - model_name: mistral-large-proxy
  #   litellm_params:
  #     model: openai/mistral-large
  #     api_base: http://localhost:8000/v1
  #     api_key: "none"
  #     timeout: 60

litellm_settings:
  set_verbose: True
  ssl_verify: false
  callbacks:
    - src.litellm_gigachat.callbacks.content_handler.gigachat_transformer_instance
    - src.litellm_gigachat.callbacks.token_callback.gigachat_callback_instance
    - src.litellm_gigachat.callbacks.proxy_provider_callback.proxy_provider_callback_instance
