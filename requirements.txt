# LiteLLM и прокси (точная версия для воспроизводимости)
litellm==1.65.1
litellm[proxy]==1.65.1

# HTTP клиент
requests>=2.31.0,<3.0.0

# Сертификаты
certifi>=2023.7.22

# Переменные окружения
python-dotenv>=1.0.0,<2.0.0

# Стандартные библиотеки (обычно не нужно указывать, но для ясности)
# importlib - встроенная библиотека Python 3.1+
